

public class event {
    //so the event needs to hold this info
    //introductory text options in an array ("book text")

    //event types:
    // enemy / inventory / information / quest (enum?)

    // each event has requirements? like in inventory, 
    // a requirement would be that you do not have the item in your inventory, depending on the item
}
